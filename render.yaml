services:
- type: web
  name: dashboard
  env: python
  plan: free
  buildCommand: pip install -r requirements.txt
  startCommand: gunicorn  --chdir src setup:server
  envVars:
  - key: FLASK_ENV
    value: production
